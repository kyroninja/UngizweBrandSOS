# Runs worker.py every 2 minutes. Env vars are sourced from /app/env.sh
# because cron does not inherit the container's environment automatically.
*/2 * * * * root . /app/env.sh && /usr/local/bin/python /app/worker.py >> /var/log/worker.log 2>&1
